apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "blue-whale.fullname" . }}-test-connection"
  labels:
    {{- include "blue-whale.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "blue-whale.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
