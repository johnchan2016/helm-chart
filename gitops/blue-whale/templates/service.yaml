apiVersion: v1
kind: Service
metadata:
  name: {{ include "blue-whale.fullname" . }}
  labels:
    {{- include "blue-whale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blue-whale.selectorLabels" . | nindent 4 }}
